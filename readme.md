# Jeu de Deviner le Nombre (TypeScript + Vite)

Un petit jeu simple dans le navigateur où il faut deviner un nombre.  
Construit avec **TypeScript** et **Vite**.

## Démo

Entrez un nombre dans la plage indiquée et essayez de deviner le nombre correct.  
Le jeu vous donnera un retour sur chaque tentative et suivra le nombre d'essais.  

## Fonctionnalités

- Saisir un nombre dans une plage définie
- Affichage des retours pour chaque tentative
- Compteur du nombre d'essais
- Bouton pour recommencer le jeu
- Développé avec TypeScript, HTML et CSS vanilla

## Installation

1. Cloner le dépôt :

```bash
git clone https://github.com/votre-username/vanilla-ts-number-game.git
cd vanilla-ts-number-game
```

2. Installer les dépendances :

```bash
npm install
# ou
yarn
# ou
pnpm install
```

3. Lancer le serveur de développement :

```bash
npm run dev
# ou
yarn dev
# ou
pnpm dev
```

4. Ouvrir votre navigateur sur `http://localhost:5173` (ou l’URL indiquée dans le terminal).

## Structure du projet

```
.
├── index.html
├── package.json
├── package-lock.json
├── .gitignore
└── src/
    └── main.ts
```

## Licence

MIT

